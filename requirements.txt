# torch is NOT listed here -- docker/Dockerfile's base image
# (pytorch/pytorch:2.8.0-cuda12.8-cudnn9-runtime) already has the right
# CUDA-matched torch build baked in. Keep it out of this file so `pip
# install -r` never silently pulls a CPU-only wheel over it.
datasets>=2.19.0
# Pinned below 1.0: huggingface_hub's 1.x line switched its HTTP backend to
# httpx, whose retry path can try to reuse an httpx client that already got
# closed after a dropped connection, crashing with "Cannot send a request,
# as the client has been closed" instead of actually retrying. The mature
# 0.x (requests-based) series doesn't have this bug and datasets>=2.19
# already accepts it (huggingface-hub<2.0,>=0.25.0).
huggingface_hub<1.0,>=0.25.0
tokenizers>=0.19.0
streamlit>=1.35.0
pyyaml>=6.0
numpy>=1.26.0
tqdm>=4.66.0
