# torch is installed separately in docker/Dockerfile (CUDA build matched to
# the host GPU) -- keep it out of this file so `pip install -r` never
# silently pulls a CPU-only wheel over it.
datasets>=2.19.0
tokenizers>=0.19.0
streamlit>=1.35.0
pyyaml>=6.0
numpy>=1.26.0
tqdm>=4.66.0
